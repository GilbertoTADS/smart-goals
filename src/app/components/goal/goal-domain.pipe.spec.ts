import { GoalDomainPipe } from './goal-domain.pipe';

describe('GoalDomainPipe', () => {
  it('create an instance', () => {
    const pipe = new GoalDomainPipe();
    expect(pipe).toBeTruthy();
  });
});
